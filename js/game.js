/*  Global variable used in multiple scenes */
let selected_option;

let community_id;

let total_co2 = 0;
let infrastructure_co2 = 0;

let money = 2000;

let zoomed_in = false;
let bot_hud;

let time = 120;  // Time limit = 2 minutes = 120 seconds

let start_timer = true;  // Start the timer as soon as game loads

let points = calculatePoints()[0];  // Initial points
